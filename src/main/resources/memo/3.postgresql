PostgreSQL 설치 및 실행 (docker)

docker run -p 5432:5432 -e POSTGRES_PASSWORD=pass -e POSTGRES_USER=hyunsooryu -e POSTGRES_DB=springboot --name postgres_boot -d postgres

docker exec -i -t postgres bash

su - postgres

psql springboot

데이터베이스 조회
\list

테이블 조회
\dt

쿼리
SELECT * FROM account;